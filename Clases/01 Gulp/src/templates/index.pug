<!DOCTYPE html>
html(lang="es")
head
  meta(charset="UTF-8")
  title Rianjani
  each css in files.CSS
    link(rel="stylesheet" href=`${css}`)

body
  h1.titulo Ya funciona wii!!!

  each js in files.JS
    script(src=`${js}`)
